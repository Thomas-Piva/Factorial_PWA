interface:
  display_name: "Factorial PWA"
  short_description: "Repo-specific patterns and workflows for Factorial_PWA"
  default_prompt: "Use the Factorial_PWA repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true